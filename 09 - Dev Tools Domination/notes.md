element inspect > break on 

console.log('%c value, inline css (i.e. 'font-size: 10px');

can use console.group(`${varName}`); to group console logs by the variable
-w/ console.groupCollapsed(`${varName}`);
-w/ console.groupEnd(`${varName}`);